// This file is used by the login function in test/pages/login.
// Ex. loginPage.login('user') would login the user with the
// 'user' credentials below, while loginPage.login('admin')
// would login the 'admin' user with credentials listed below.

module.exports = {
    credentials: {
        user: 'pass',
        admin: 'pass'
    }
};
